# md = """
# > This is a quote
# > that spans multiple lines
# > and should be treated
# > as one block
# """
#
# rm = "> "
# formatted = md.replace(rm, "").lstrip().rstrip()
#
# print(repr(formatted))